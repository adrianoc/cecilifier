using System;

public class GenericClass<T>
{
    public T theField;
    public T TheProperty  { get; set; }
    public T TheMethod(T p) { return p; }
    
    //public event EventHandler<T> TheEvent;    
    //public delegate T TheDelegate(T t);
}

public struct GenericStruct<T> 
{
    public T theField;
    public T TheProperty  { get; set; }
    public T TheMethod(T p) { return p; }
    
    //public event EventHandler<T> TheEvent;    
    //public delegate T TheDelegate(T t);
}


public struct GenericInterface<T> 
{
    T TheProperty  { get; set; }
    T TheMethod(T p) { return p; }
    //event EventHandler<T> TheEvent;
}