struct S
{
    int i;
    byte b;
}

class StackAlloc
{
    private unsafe void M(int n)
	{
	    int *i = stackalloc int[10];
	    S *s = stackalloc S[n];
	    S *s2 = stackalloc S[42];
	    
	    Dummy(i, s, s2);
	}
	
	private unsafe void Dummy(int* i, S* s, S* s2) {}
}